# V0.8.0 2v2 候选静态检查报告

日期：2026-09-25

## 已完成

- 从本地 `main` 基线 `src/release-v071.snapshot.html` 构建单文件候选，并嵌入 2v2 规则与运行时。
- 构建检查：172 个唯一 DOM ID，所有 `$()` 直接元素引用存在；候选中所有内联脚本通过 Node.js 语法检查。
- Worker 检查：`turn-worker/src/index.js` 通过 `node --check`；`git diff --check` 未发现空白错误。
- 客机司机的快照校正现在会按误差与 RTT 平滑收敛；受击、瘫痪、起火等关键状态变化会硬校正。炮弹弹道按对方车辆运动预判。
- Cloudflare 房间目录请求支持 `gameMode`，2v2 房间要求 `maxPlayers=4`。先前部署返回 `https://api.feelpal.app/rooms` HTTP 200；本轮未更改 Worker 凭证端点或 Secret。

## 未验收

- 未运行桌面或手机 Chromium 回归，也未运行四个真实客户端联机。
- 未验证真实 P2P/TURN 数据通道、炮弹命中率、驾驶碰撞/交通碰撞及长局状态同步。
- 因此没有 400ms 模拟结果，也不宣称线上可玩、真实 RTT 降低或 TURN 已通过。

## 发布建议

完成上述联机验收并修复发现的问题后，再替换 `index.html` 并推送 GitHub Pages。当前文件仅为可审阅候选。
