# V0.8.0 2v2 候选检查报告

日期：2026-09-25

## 已完成

- 修复对战入口消失的启动错误：2v2 规则与适配器原先被拼接在游戏主 IIFE 外，无法访问 `roomId` 等内部变量。构建器现在将规则和适配器放在游戏主作用域内。
- 构建检查：172 个唯一 DOM ID，所有 `$()` 直接元素引用存在；内联脚本通过 Node.js 语法检查。
- 本地 Chromium 菜单检查通过：可见“车队合作 / 2v2 对抗”；选择 2v2 后人数控件显示固定四人，2 人和 3 人选项被禁用。
- `turn-worker/src/index.js` 通过 `node --check`；`git diff --check` 无空白错误；根 `index.html` 与候选 HTML 一致。
- 本次修复未修改 Cloudflare Worker 的凭证端点或 Secret，也没有在页面或源码中加入 TURN 临时凭证。

## 尚未验收

- 未验证四客户端真实房间联机，也未完成桌面/手机完整回归。
- 未实测 WebRTC/TURN、碰撞、投递和维修全流程。
- 未运行 0/80/150/250/400ms 延迟模拟，因此没有 400ms 模拟结果。

## 发布状态

此次提交只修复并推送代码。GitHub Pages 是否已完成部署需要等待站点更新；四客户端对战仍需实际联测。
