--- v0.7.0/index.html
+++ v0.7.1/index.html
@@ -5,7 +5,7 @@
 <meta name="theme-color" content="#162e34"><meta name="apple-mobile-web-app-capable" content="yes">
 <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
 <meta name="description" content="暴力快递 V0.7 城市交通：住宅、商业、工业三区，18 件任务包裹，路口转弯让行、事故清障及双人同车合作。">
-<title>暴力快递 · V0.7 ONLINE · CITY ROUTES</title>
+<title>暴力快递 · V0.7.1 ONLINE · PUBLIC NET</title>
 <style>:root{--ink:#16343a;--deep:#142b31;--gold:#ffd05b;--cream:#fff4d7;--muted:#abc2c4;--safeB:env(safe-area-inset-bottom,0px);--safeT:env(safe-area-inset-top,0px);--safeL:env(safe-area-inset-left,0px);--safeR:env(safe-area-inset-right,0px)}*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:var(--deep);color:var(--cream);font-family:Arial,"PingFang SC","Microsoft YaHei",sans-serif;user-select:none;-webkit-user-select:none;overscroll-behavior:none}button{font:inherit;color:inherit;cursor:pointer;border:0;-webkit-tap-highlight-color:transparent;touch-action:none}button:focus-visible,canvas:focus-visible{outline:2px solid var(--gold);outline-offset:3px}button:disabled{opacity:.42;cursor:default}button:active{transform:translateY(1px)}.hidden{display:none!important}#game,#world,#overlay,#hud,#vignette{position:absolute;inset:0;width:100%;height:100%}#world{touch-action:none;display:block;outline:none}#overlay,#hud,#vignette{pointer-events:none}#vignette{background:linear-gradient(#133c4235,transparent 24%,transparent 74%,#142b3140)}#hud button,#hud .joystick{pointer-events:auto}.control{background:#16343ae8;border:1px solid #ffffff36;border-radius:15px;box-shadow:0 2px 0 #0e292849;min-width:44px;min-height:44px;display:flex;align-items:center;justify-content:center}.control.pressed,.control.active{background:var(--gold);color:var(--ink);border-color:#ffe6a6}.statusbar{position:absolute;left:max(16px,var(--safeL));top:calc(10px + var(--safeT));height:44px;display:flex;align-items:center;gap:17px;border-radius:13px;background:#142f37e5;padding-left:13px;font-size:14px;border:1px solid #ffffff25}.viewtag{font-size:11px;letter-spacing:1px;color:var(--gold);padding-right:12px;border-right:1px solid #ffffff2a}.stat small{font-size:10px;color:var(--muted);margin-left:4px}.money{color:var(--gold)}.statusbar b,.time{font-variant-numeric:tabular-nums}.time{font-size:12px;color:#d2e0dc}.pause{border:0;border-left:1px solid #ffffff25;border-radius:0 12px 12px 0;background:transparent;font-size:18px}.route{position:absolute;top:calc(62px + var(--safeT));left:max(16px,var(--safeL));height:35px;max-width:310px;display:flex;align-items:center;gap:11px;background:#16343ad2;border-radius:9px;padding:0 12px;font-size:12px}.route>b{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-weight:500}.route>span:first-child{color:var(--gold);font-size:17px}.route>span:last-child{color:var(--gold);white-space:nowrap}.statuschips{position:absolute;left:max(17px,var(--safeL));top:calc(105px + var(--safeT));display:flex;gap:5px;flex-wrap:wrap;max-width:calc(100% - 120px)}.statuschips>span{background:#17373ac9;border:1px solid #ffffff12;padding:4px 7px;border-radius:5px;font-size:9px;white-space:nowrap}.statuschips>.warn{background:#9f552ce8;color:#fff2cf}.statuschips>.on{color:#ffdb81}.mapPanel{position:absolute;right:max(17px,var(--safeR));top:calc(12px + var(--safeT));width:106px;padding:6px;border-radius:12px;background:#16343adb;border:1px solid #ffffff22}.mapPanel canvas{width:100%;height:auto;display:block;border-radius:7px}.mapPanel small{display:block;font-size:9px;color:#b1c8c5;text-align:center;margin:4px 0 1px}.mapPanel small span{color:var(--gold)}#risk{position:absolute;left:50%;top:calc(20px + var(--safeT));transform:translateX(-50%);background:#934829ec;border:1px solid #ffd68e77;border-radius:8px;padding:8px 12px;font-size:12px;white-space:nowrap}#toast{position:absolute;top:23%;left:50%;transform:translate(-50%,-8px);opacity:0;transition:opacity .15s,transform .15s;background:#16343af2;border:1px solid #ffd05b5b;max-width:min(370px,85%);min-width:230px;padding:15px 22px;border-radius:13px;text-align:center}#toast.show{opacity:1;transform:translate(-50%,0)}#toast b{display:block;color:var(--gold);font-size:24px}#toast span{display:block;font-size:11px;line-height:1.7;margin-top:5px;color:#dce8df}#hint{position:absolute;bottom:calc(154px + var(--safeB));left:50%;transform:translateX(-50%);background:#17343acf;color:#f7f2d9;border-radius:10px;padding:7px 12px;font-size:11px;line-height:1.55;text-align:center;max-width:min(430px,70%);width:max-content}.telemetry{position:absolute;left:50%;bottom:calc(45px + var(--safeB));transform:translateX(-50%);display:flex;align-items:baseline;gap:4px;text-shadow:0 2px 10px #17373a}.telemetry strong{font-size:42px;line-height:1;font-variant-numeric:tabular-nums;letter-spacing:-1px}.telemetry small{font-size:9px;color:#d0e0dc}.healthtrack{position:absolute;width:70px;bottom:-12px;left:0;height:3px;background:#1c3439b0;border-radius:3px;overflow:hidden}.healthtrack i{display:block;height:100%;width:100%;background:#afd5b5}.driveLeft{position:absolute;left:max(23px,var(--safeL));bottom:calc(25px + var(--safeB))}.driveRight{position:absolute;right:max(23px,var(--safeR));bottom:calc(25px + var(--safeB));display:flex;flex-direction:column;align-items:flex-end;gap:10px}.steering,.pedals{display:flex;gap:8px}.steering button{width:64px;height:58px;font-size:21px}.pedals button{width:64px;height:58px;font-size:12px}.pedals .gas{background:#eff0dfe8;color:var(--ink);border-color:#faf9dd}.pedals .gas.pressed{background:var(--gold)}.driveTools{margin-bottom:9px;display:flex;gap:7px}.tool{gap:5px;padding:0 13px;font-size:18px}.tool small{font-size:11px}.enter{gap:7px;background:var(--gold);color:var(--ink);padding:0 13px;height:48px;border-color:#ffe7a7}.enter span{font-size:24px}.enter b{font-size:12px}.keyhelp{position:absolute;bottom:6px;left:50%;transform:translateX(-50%);font-size:10px;color:#fff5d8bd;white-space:nowrap}kbd{font:inherit;font-size:10px;opacity:.65;border:1px solid currentColor;padding:1px 4px;border-radius:4px;margin-left:3px}.joystick{position:absolute;width:128px;height:128px;left:max(23px,var(--safeL));bottom:calc(26px + var(--safeB));touch-action:none}.stickring{position:absolute;inset:5px;background:#173b3ba9;border:1px solid #ffffff3f;border-radius:50%;pointer-events:none}.stickthumb{position:absolute;left:44px;top:44px;width:40px;height:40px;border-radius:50%;background:#e1e9dabb;border:1px solid #ffffff6a;pointer-events:none}.joystick small{position:absolute;left:0;right:0;bottom:-7px;text-align:center;font-size:8px;color:#fff4d5a0;pointer-events:none}.walk{position:absolute;background:transparent;border:0;width:44px;height:44px;font-size:10px;color:#fff9df8a;border-radius:50%;touch-action:none}.walk.up{left:42px;top:-2px}.walk.down{left:42px;bottom:-2px}.walk.left{left:-2px;top:42px}.walk.right{right:-2px;top:42px}.walk.pressed{background:#ffd05b66;color:#fff4d5}.cargoActions{position:absolute;right:max(23px,var(--safeR));bottom:calc(25px + var(--safeB));display:flex;flex-direction:column;align-items:flex-end;gap:7px}.smallAct{font-size:11px;border-radius:10px;min-width:66px;padding:0 10px;min-height:44px}.interact{background:#f2f1deef;color:var(--ink);gap:6px;padding:0 14px;height:49px;font-size:12px}.interact span{font-size:18px}.throw{background:var(--gold);color:var(--ink);width:82px;height:80px;border-radius:23px;border:2px solid #ffe6a6;flex-direction:column;gap:1px;box-shadow:0 4px 0 #a9833999}.throw span{font-size:30px;line-height:28px}.throw b{font-size:13px}.throw.charging{background:#ffefaa;transform:translateY(2px);box-shadow:none}.powertrack{width:82px;height:4px;background:#102e32ad;border-radius:3px;overflow:hidden}.powertrack i{display:block;width:0;height:100%;background:var(--gold)}.rearControls{position:absolute;left:50%;bottom:calc(91px + var(--safeB));transform:translateX(-50%);text-align:center}.rearControls small{font-size:8px;display:block;color:#ebf1d8;margin-bottom:4px;text-shadow:0 1px 4px #1c3037}.rearControls>div{display:flex;gap:5px}.doorToggle{font-size:11px;min-width:44px;min-height:44px;border-radius:10px}.doorToggle.open{background:#955c30dd;border-color:#e7ad7150}#crosshair{position:absolute;top:50%;left:50%;width:18px;height:18px;transform:translate(-50%,-50%);border:1px solid #fff6d99c;border-radius:50%;box-shadow:0 0 3px #193d41}#crosshair i{position:absolute;width:3px;height:3px;border-radius:50%;background:#fff1b2;top:6px;left:6px}#crosshair.locked{border-color:#ffd05b;width:25px;height:25px}#crosshair.locked i{left:10px;top:10px}#focusLabel{position:absolute;top:calc(50% + 22px);left:50%;transform:translateX(-50%);font-size:11px;background:#123339d9;border-radius:6px;padding:5px 8px;white-space:nowrap;max-width:90%;text-overflow:ellipsis;overflow:hidden}#focusLabel:empty{display:none}.screen{position:absolute;inset:0;z-index:10}.menuScreen{padding:36px 7%;display:flex;align-items:center;background:linear-gradient(90deg,#112d34f5,#112d34e7 27%,#112d3480 51%,#112d3400 77%)}.menuCopy{max-width:490px}.eyebrow{color:#a5c2c2;font-size:9px;letter-spacing:2px;display:flex;align-items:center;gap:8px}.eyebrow i{width:6px;height:6px;background:var(--gold);border-radius:50%}.menuCopy h1{font-size:66px;letter-spacing:-3px;line-height:1.05;margin:22px 0 8px;font-weight:900;white-space:nowrap}.menuCopy h1 span{color:var(--gold)}.menuCopy h1 sup{font-size:42px;margin-left:7px}.englishTitle{font-size:12px;color:#c0d2cd;letter-spacing:6px}.edition{font-size:10px;letter-spacing:2px;margin:22px 0 0;color:#ffd972;display:flex;align-items:center;gap:11px}.edition span{padding:5px 9px;border:1px solid #ffd05b4a;border-radius:4px;font-size:10px;letter-spacing:3px}.menuCopy h2{font-size:39px;line-height:1.27;letter-spacing:1px;margin:15px 0 18px}.menuCopy p{font-size:13px;line-height:1.9;color:#c2d5cf;margin:0 0 25px}.menuCopy em{font-style:normal;color:#e9d4a6}.primary{display:block;border-radius:10px;width:100%;max-width:332px;padding:16px 19px;min-height:50px;background:var(--gold);color:var(--ink);font-size:15px;font-weight:800;text-align:left}.secondary{display:block;background:transparent;color:#c8d9d4;font-size:12px;padding:14px 0;min-height:44px;text-align:left}.menuFooter{font-size:9px;display:flex;gap:18px;color:#8cacae;flex-wrap:wrap;margin-top:8px}.menuStamp{position:absolute;right:7%;bottom:12%;padding-left:17px;border-left:3px solid var(--gold);text-shadow:0 2px 10px #15333d}.menuStamp>span{font-size:11px;letter-spacing:4px;color:#f9e6b3}.menuStamp strong{display:block;font-size:32px;line-height:1.4;margin:13px 0}.menuStamp small{font-size:10px}.modalScreen{background:#112830e8;display:flex;align-items:center;justify-content:center;padding:max(15px,var(--safeT)) 17px max(15px,var(--safeB));backdrop-filter:blur(6px)}.modal{background:#1a3940;border:1px solid #ffffff1c;border-radius:20px;padding:28px;max-width:470px;width:100%;max-height:100%;overflow:auto}.modal h2{font-size:25px;line-height:1.3;margin:16px 0}.modal p{font-size:12px;color:#b6cecb;line-height:1.8}.modal .primary{max-width:none}.modal>.primary{margin-top:20px}.settings{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:18px}.settings button{background:#ffffff0b;border:1px solid #ffffff22;border-radius:9px;padding:12px 7px;font-size:11px;min-height:46px}.fineprint{font-size:10px!important;color:#90b2b3!important}.resolution{font-size:10px!important;color:#e6d5a2!important;letter-spacing:.4px}.helpRows>div{border-bottom:1px solid #ffffff13;padding:10px 0}.helpRows b{color:var(--gold);font-size:12px}.helpRows p{font-size:11px;margin:5px 0 0}.endMark{color:var(--gold);font-size:54px}.endStats{display:flex;justify-content:space-between;gap:18px;margin:18px 0}.endStats small{color:var(--muted);display:block;font-size:10px}.endStats strong{color:var(--gold);display:block;font-size:24px;margin-top:7px}.noscript{position:absolute;inset:0;background:var(--deep);padding:40px;display:grid;place-items:center;z-index:99}
 @media(hover:hover){button:not(:disabled):hover{filter:brightness(1.1)}}
 @media(pointer:coarse),(max-width:760px){kbd,.keyhelp{display:none}.statusbar{left:max(10px,var(--safeL));top:calc(8px + var(--safeT));gap:13px;height:42px;padding-left:11px;font-size:13px}.viewtag{font-size:10px;padding-right:9px}.route{left:max(10px,var(--safeL));top:calc(57px + var(--safeT));font-size:11px;max-width:270px;height:32px}.statuschips{left:max(11px,var(--safeL));top:calc(96px + var(--safeT))}.statuschips>span{font-size:8px;padding:3px 6px}.mapPanel{width:78px;right:max(10px,var(--safeR));top:calc(9px + var(--safeT));padding:5px;border-radius:10px}.mapPanel small{font-size:7px}.driveLeft{left:max(12px,var(--safeL));bottom:calc(15px + var(--safeB))}.driveRight{right:max(12px,var(--safeR));bottom:calc(15px + var(--safeB));gap:8px}.steering,.pedals{gap:6px}.steering button,.pedals button{width:52px;height:52px;border-radius:14px}.pedals button{font-size:11px}.steering button{font-size:18px}.tool{min-height:44px;font-size:17px;padding:0 10px;gap:5px}.tool small{font-size:10px}.enter{height:46px;min-width:108px;padding:0 11px;border-radius:13px}.enter b{font-size:11px}.enter span{font-size:19px}.driveTools{margin-bottom:8px}.telemetry{bottom:calc(33px + var(--safeB))}.telemetry strong{font-size:33px}.telemetry small{font-size:8px}.healthtrack{width:56px;bottom:-9px}#hint{bottom:calc(151px + var(--safeB));font-size:10px;max-width:75%;padding:7px 10px}#toast{top:24%;min-width:200px;padding:12px 17px}#toast b{font-size:21px}#toast span{font-size:10px}#risk{top:calc(125px + var(--safeT));font-size:10px;max-width:90%;padding:6px 10px}.joystick{left:max(9px,var(--safeL));bottom:calc(15px + var(--safeB));transform:scale(.92);transform-origin:left bottom}.cargoActions{right:max(12px,var(--safeR));bottom:calc(15px + var(--safeB));gap:6px}.throw{width:76px;height:73px;border-radius:22px}.throw span{font-size:28px}.powertrack{width:76px}.interact{padding:0 10px;font-size:11px;height:46px}.interact span{font-size:16px}.smallAct{font-size:10px;min-width:65px;min-height:44px}.rearControls{bottom:calc(98px + var(--safeB))}.rearControls>div{gap:3px}.doorToggle{min-width:44px;min-height:44px}.rearControls small{font-size:8px}#focusLabel{font-size:10px}.mapPanel small{margin:2px 0 0}}
@@ -101,6 +101,11 @@
 @media(pointer:coarse),(max-width:760px){#game[data-online=true] #risk{top:calc(194px + var(--safeT))}}
 @media(max-height:540px) and (orientation:landscape){#game[data-online=true] #risk{top:calc(8px + var(--safeT));left:60%}}

+
+/* V0.7.1 public-network diagnostics / TURN UI */
+.netTools{max-width:332px;display:flex;gap:7px;margin-top:7px}.netTools button{flex:1;min-height:36px;border:1px solid #ffffff1d;border-radius:8px;background:#ffffff07;color:#bdd0d0;font-size:9px}.netTools button strong{color:#ffe09a;font-weight:600}.netStatusPill[data-state=ok]:before{background:#75d5a0}.netStatusPill[data-state=warn]:before{background:#e3b860}.netStatusPill[data-state=bad]:before{background:#e07d6c}.netStatusPill[data-state=working]:before{background:#72b9d2;box-shadow:0 0 0 3px #72b9d220}.networkModal .netGrid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}.networkModal label{font-size:10px;color:#a9c2c3;display:flex;flex-direction:column;gap:5px}.networkModal label.wide{grid-column:1/-1}.networkModal input,.networkModal select{width:100%;min-height:42px;border:1px solid #ffffff2b;border-radius:8px;background:#102c33;color:#fff4d7;padding:0 10px;font:inherit;font-size:13px;outline:none}.networkModal input:focus,.networkModal select:focus{border-color:#ffd05b88}.networkModal .netActions{display:flex;gap:7px;flex-wrap:wrap;margin-top:12px}.networkModal .netActions button{flex:1;min-width:125px}.netDiag{margin-top:12px;border:1px solid #ffffff16;border-radius:10px;background:#102c3370;padding:10px;font-size:10px;color:#b5cbca;line-height:1.7}.netDiag b{color:#ffe09a}.netDiag code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#e9f2e8}.netSecret{font-size:9px!important;color:#89a9ab!important}.linkBadge{display:inline-flex;align-items:center;gap:4px;border:1px solid #ffffff1d;border-radius:6px;padding:2px 5px;color:#cfe0db;background:#ffffff08}.linkBadge.relay{color:#ffd487;border-color:#e6ad5045}.linkBadge.direct{color:#a5e2bd;border-color:#65be8950}.reconnectNote{color:#f4cf85}.iceTypes{display:flex;gap:5px;flex-wrap:wrap;margin-top:6px}.iceTypes span{border:1px solid #ffffff1e;border-radius:6px;padding:3px 6px;background:#ffffff06}.iceTypes .relay{color:#ffd487}.iceTypes .srflx{color:#a5e2bd}.iceTypes .host{color:#b7c9ca}
+@media(max-width:600px) and (orientation:portrait){.netTools{max-width:none;width:100%}.networkModal .netGrid{grid-template-columns:1fr}.networkModal label.wide{grid-column:auto}.networkModal{padding:20px 16px!important}.netDiag{font-size:9px}.netTools button{min-height:40px}}
+@media(max-height:540px) and (orientation:landscape){.netTools{position:absolute;left:315px;top:348px;width:110px;display:block;margin:0}.netTools button{width:100%;min-height:28px;margin-bottom:4px;font-size:8px}.networkModal{max-width:650px}.networkModal .netGrid{grid-template-columns:repeat(3,1fr)}.networkModal label.wide{grid-column:auto}.networkModal .netDiag{max-height:92px;overflow:auto}}
 </style>
 </head>
 <body>
@@ -130,13 +135,14 @@
  </div>
  <section id="manifest" class="manifest hidden" aria-label="配送清单"><header><div><small>DISPATCH / 12 JOBS</small><h2>认准门牌，再扔。</h2></div><button id="closeOrdersBtn" type="button" class="control" aria-label="关闭订单清单">×</button></header><p>点一单设置团队导航。清单打开时车辆仍会行驶。</p><div id="orderList"></div><footer id="manifestFooter">每个包裹都有唯一收件地址。</footer></section>
 <section id="menu" class="screen menuScreen">
-  <div class="menuCopy"><div class="eyebrow"><i></i> A VERY UNPROFESSIONAL DELIVERY SERVICE</div><h1>暴力<span>快递</span><sup>↗</sup></h1><div class="englishTitle">CRASH DELIVERY</div><div class="edition">V0.7 ONLINE <span>CITY ROUTES</span></div><h2>认准这箱。<br>一起送到。</h2><p>同一辆车，两名快递员。<br>18 件快递，三片街区。看路况、找包裹，和搭档一起送到。</p><div class="profileSetup"><label><b>你的快递员昵称</b><span>最多 12 个字</span></label><div class="nicknameRow"><input id="nicknameInput" maxlength="12" autocomplete="nickname" placeholder="例如：闪电小张" aria-label="你的游戏昵称"><span class="nicknameBadge"><i></i>头顶显示</span></div></div><div class="lobbyLabel">MULTIPLAYER / 双人联机</div><div class="multiplayerPanel"><button id="hostBtn" class="primary" style="background:#f1e4b5">🌐 创建双人房间</button><div class="joinRow"><input id="roomCode" maxlength="6" inputmode="numeric" placeholder="输入 6 位房间码" aria-label="6 位房间码"><button id="joinBtn" class="control">加入</button></div></div><div id="netStatus" class="netStatusPill">多人模式：等待创建或加入房间</div><button id="startBtn" class="secondary soloLink" disabled>单人练习 →</button><div class="packageLegend"><span>📦 普通</span><span>📱 小包</span><span>📺 重箱</span><span>🍷 易碎</span><span>🛢 圆筒</span><span>❓ 神秘</span></div><div class="menuLinks"><button id="wardrobeBtn" class="secondary">角色 / 动作预览 ↗</button><button id="copyRoomBtn" class="secondary hidden">复制邀请</button><button id="cancelNetBtn" class="secondary hidden">取消连接</button></div><button id="garageBtn" class="secondary">车库 / 永久升级 ↗</button><button id="cityMenuBtn" class="secondary">城市地图 / 三个街区 ↗</button><button id="helpBtn" class="secondary">怎么玩 · 操作说明 ↗</button><div class="menuFooter"><span>真 3D / 多人实验版</span><span>手机 + 电脑</span><span id="bestMenu">最高收入 $0</span></div></div>
+  <div class="menuCopy"><div class="eyebrow"><i></i> A VERY UNPROFESSIONAL DELIVERY SERVICE</div><h1>暴力<span>快递</span><sup>↗</sup></h1><div class="englishTitle">CRASH DELIVERY</div><div class="edition">V0.7.1 ONLINE <span>PUBLIC NET</span></div><h2>认准这箱。<br>一起送到。</h2><p>同一辆车，两名快递员。<br>18 件快递，三片街区。看路况、找包裹，和搭档一起送到。</p><div class="profileSetup"><label><b>你的快递员昵称</b><span>最多 12 个字</span></label><div class="nicknameRow"><input id="nicknameInput" maxlength="12" autocomplete="nickname" placeholder="例如：闪电小张" aria-label="你的游戏昵称"><span class="nicknameBadge"><i></i>头顶显示</span></div></div><div class="lobbyLabel">MULTIPLAYER / 双人联机</div><div class="multiplayerPanel"><button id="hostBtn" class="primary" style="background:#f1e4b5">🌐 创建双人房间</button><div class="joinRow"><input id="roomCode" maxlength="6" inputmode="numeric" placeholder="输入 6 位房间码" aria-label="6 位房间码"><button id="joinBtn" class="control">加入</button></div></div><div id="netStatus" class="netStatusPill" data-state="idle">多人模式：等待创建或加入房间</div><div class="netTools"><button id="networkBtn" type="button">🌐 网络设置</button><button id="quickIceBtn" type="button">◌ ICE 自检</button></div><button id="startBtn" class="secondary soloLink" disabled>单人练习 →</button><div class="packageLegend"><span>📦 普通</span><span>📱 小包</span><span>📺 重箱</span><span>🍷 易碎</span><span>🛢 圆筒</span><span>❓ 神秘</span></div><div class="menuLinks"><button id="wardrobeBtn" class="secondary">角色 / 动作预览 ↗</button><button id="copyRoomBtn" class="secondary hidden">复制邀请</button><button id="cancelNetBtn" class="secondary hidden">取消连接</button></div><button id="garageBtn" class="secondary">车库 / 永久升级 ↗</button><button id="cityMenuBtn" class="secondary">城市地图 / 三个街区 ↗</button><button id="helpBtn" class="secondary">怎么玩 · 操作说明 ↗</button><div class="menuFooter"><span>真 3D / 公网联机实验版</span><span>手机 + 电脑</span><span id="bestMenu">最高收入 $0</span></div></div>
   <div class="menuStamp"><span>CREW / 02</span><strong>一个看路。<br>一个送到。</strong><small>独立玩家 · 实体角色 · 同车协作</small></div>
  </section>
- <section id="pauseScreen" class="screen modalScreen hidden"><div class="modal"><div class="eyebrow">PIT STOP / 暂停</div><h2>喘口气。车也暂停了。</h2><p id="pauseSummary"></p><p id="pauseNet" class="fineprint"></p><button id="resumeBtn" class="primary">继续派送 →</button><div class="settings"><button id="qualityBtn">画质：自动高清</button><button id="assistBtn">辅助瞄准：开</button><button id="soundBtn">音效：开</button><button id="motionBtn">镜头晃动：轻微</button><button id="serviceBtn">道路救援 / 救援 / 补齐订单</button><button id="restartBtn">重新开始本轮</button></div><p class="resolution" id="resolutionLabel"></p><p class="fineprint">救援保留已送订单，将车辆停回道路、修复并补齐未完成订单的包裹（不会复制队友手中的包裹）；收入扣 $120（最低为 0），用时 +20 秒。不会自动替你关闭后门。</p><button id="leaveBtn" class="secondary hidden">退出联机 / 返回菜单</button><button id="pauseHelpBtn" class="secondary">查看操作说明</button></div></section>
- <section id="garageScreen" class="screen modalScreen hidden"><div class="modal"><div class="eyebrow">GARAGE / 永久升级</div><h2>把今天赚的钱，花在明天的事故上。</h2><p>当前车库资金：<strong id="garageCredits" style="color:var(--gold)">$0</strong></p><div class="settings"><button id="upgrade_engine"><b>发动机 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_brakes"><b>刹车 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_grip"><b>抓地力 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_mat"><b>货箱防滑 Lv.0</b><br><span>升级 $300</span></button></div><p class="fineprint">发动机提高加速；刹车缩短制动距离；抓地力改善高速转向；防滑垫降低货箱内包裹滑动。升级永久保存在本机。</p><button id="closeGarageBtn" class="primary">返回主菜单 →</button></div></section>
- <section id="cityScreen" class="cityScreen hidden" aria-label="城市地图"><div class="citySheet"><header><div><small>V0.7 / CITY ROUTES</small><h2>认路，再出发。</h2></div><button id="closeCityMapBtn" class="control" type="button" aria-label="关闭城市地图">×</button></header><div class="cityLayout"><div class="cityMapBox"><canvas id="cityMap" width="640" height="640" aria-label="城市道路与任务地址。点击编号选择地址；也可使用订单面板选择。"></canvas><p class="cityLegend">黄线：团队路线　橙虚线：事故路　浅色方块：包裹地址<br>点击编号查看详情。路线只作提示，不会替你转向。</p></div><div class="cityInfo"><div id="cityDistricts" class="cityDistricts"><button type="button" data-district="all" class="active">全部</button><button type="button" data-district="garden">住宅区</button><button type="button" data-district="market">商业街</button><button type="button" data-district="works">工业区</button></div><h3 id="cityMapRoute">向阳住宅区</h3><p id="cityMapDetail"></p><p id="cityRouteSummary"></p><p id="cityMapTraffic"></p><button id="cityNavigateBtn" class="primary" type="button">设为团队下一单 →</button><p class="cityNote">地图不会暂停联机世界。车在继续行驶，打开前请先停车或让搭档掌舵。更改导航会同步给队友。</p></div></div></div></section>
-<section id="helpScreen" class="screen modalScreen hidden"><div class="modal helpModal"><div class="eyebrow">QUICK START / 上岗指南</div><h2>搭档一起，亲手投递。</h2><div class="helpRows"><div><b>V0.7 城市交通 / N 打开地图</b><p>18 个地址分布在住宅、商业、工业三区。NPC 会沿车道转弯、等红灯、排队与让行；事故车辆会堵住道路，清障后车流恢复。黄色路线只作提示，不会自动转向。点城市地图编号或订单选择下一单。</p></div>
+ <section id="pauseScreen" class="screen modalScreen hidden"><div class="modal"><div class="eyebrow">PIT STOP / 暂停</div><h2>喘口气。车也暂停了。</h2><p id="pauseSummary"></p><p id="pauseNet" class="fineprint"></p><button id="resumeBtn" class="primary">继续派送 →</button><div class="settings"><button id="qualityBtn">画质：自动高清</button><button id="assistBtn">辅助瞄准：开</button><button id="soundBtn">音效：开</button><button id="motionBtn">镜头晃动：轻微</button><button id="pauseNetworkBtn">网络 / TURN 设置</button><button id="serviceBtn">道路救援 / 救援 / 补齐订单</button><button id="restartBtn">重新开始本轮</button></div><p class="resolution" id="resolutionLabel"></p><p class="fineprint">救援保留已送订单，将车辆停回道路、修复并补齐未完成订单的包裹（不会复制队友手中的包裹）；收入扣 $120（最低为 0），用时 +20 秒。不会自动替你关闭后门。</p><button id="leaveBtn" class="secondary hidden">退出联机 / 返回菜单</button><button id="pauseHelpBtn" class="secondary">查看操作说明</button></div></section>
+ <section id="networkScreen" class="screen modalScreen hidden"><div class="modal networkModal"><div class="eyebrow">PUBLIC NETWORK / 公网联机</div><h2>先直连，必要时走中继。</h2><p>默认使用 STUN 尝试 P2P。要让严格 NAT / 蜂窝网络也更可靠，请填入你自己的 TURN 服务。TURN 凭据只交给浏览器 WebRTC，不会写进房间消息。</p><div class="netGrid"><label><span>连接策略</span><select id="netPolicy"><option value="auto">自动：P2P 优先，TURN 兜底</option><option value="direct">仅直连 / STUN</option><option value="relay">强制 TURN 中继</option></select></label><label><span>TURN 地址</span><input id="turnUrl" autocomplete="off" placeholder="turn:turn.example.com:3478"></label><label><span>TURN 用户名</span><input id="turnUser" autocomplete="off" placeholder="username"></label><label><span>TURN 密码 / credential</span><input id="turnPass" type="password" autocomplete="new-password" placeholder="credential"></label><label class="wide"><span>额外 STUN（可留空）</span><input id="extraStun" autocomplete="off" placeholder="stun:stun.example.com:3478"></label></div><p class="netSecret">提示：共享 TURN 账号可能产生流量费用。这个版本不会内置任何第三方 TURN 密钥。</p><div class="netActions"><button id="saveNetworkBtn" class="primary" type="button">保存到本机</button><button id="testIceBtn" class="secondary" type="button">测试 ICE / TURN</button><button id="clearTurnBtn" class="secondary" type="button">清除 TURN 凭据</button></div><div id="iceDiag" class="netDiag">尚未测试。<br>看到 <b>srflx</b> 说明 STUN 获取到公网候选；看到 <b>relay</b> 才表示 TURN 中继可用。</div><button id="closeNetworkBtn" class="secondary" type="button">返回</button></div></section>
+<section id="garageScreen" class="screen modalScreen hidden"><div class="modal"><div class="eyebrow">GARAGE / 永久升级</div><h2>把今天赚的钱，花在明天的事故上。</h2><p>当前车库资金：<strong id="garageCredits" style="color:var(--gold)">$0</strong></p><div class="settings"><button id="upgrade_engine"><b>发动机 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_brakes"><b>刹车 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_grip"><b>抓地力 Lv.0</b><br><span>升级 $300</span></button><button id="upgrade_mat"><b>货箱防滑 Lv.0</b><br><span>升级 $300</span></button></div><p class="fineprint">发动机提高加速；刹车缩短制动距离；抓地力改善高速转向；防滑垫降低货箱内包裹滑动。升级永久保存在本机。</p><button id="closeGarageBtn" class="primary">返回主菜单 →</button></div></section>
+ <section id="cityScreen" class="cityScreen hidden" aria-label="城市地图"><div class="citySheet"><header><div><small>V0.7.1 / CITY ROUTES</small><h2>认路，再出发。</h2></div><button id="closeCityMapBtn" class="control" type="button" aria-label="关闭城市地图">×</button></header><div class="cityLayout"><div class="cityMapBox"><canvas id="cityMap" width="640" height="640" aria-label="城市道路与任务地址。点击编号选择地址；也可使用订单面板选择。"></canvas><p class="cityLegend">黄线：团队路线　橙虚线：事故路　浅色方块：包裹地址<br>点击编号查看详情。路线只作提示，不会替你转向。</p></div><div class="cityInfo"><div id="cityDistricts" class="cityDistricts"><button type="button" data-district="all" class="active">全部</button><button type="button" data-district="garden">住宅区</button><button type="button" data-district="market">商业街</button><button type="button" data-district="works">工业区</button></div><h3 id="cityMapRoute">向阳住宅区</h3><p id="cityMapDetail"></p><p id="cityRouteSummary"></p><p id="cityMapTraffic"></p><button id="cityNavigateBtn" class="primary" type="button">设为团队下一单 →</button><p class="cityNote">地图不会暂停联机世界。车在继续行驶，打开前请先停车或让搭档掌舵。更改导航会同步给队友。</p></div></div></div></section>
+<section id="helpScreen" class="screen modalScreen hidden"><div class="modal helpModal"><div class="eyebrow">QUICK START / 上岗指南</div><h2>搭档一起，亲手投递。</h2><div class="helpRows"><div><b>V0.7.1 公网联机 / TURN 兜底</b><p>菜单「网络设置」可检查 ICE。默认先用 STUN 直连；配置自己的 TURN 后，严格 NAT / 蜂窝网络无法直连时浏览器可使用 relay 中继。房间 HUD 会显示 P2P / TURN 与延迟；断线会尝试自动重连。</p></div><div><b>V0.7 城市交通 / N 打开地图</b><p>18 个地址分布在住宅、商业、工业三区。NPC 会沿车道转弯、等红灯、排队与让行；事故车辆会堵住道路，清障后车流恢复。黄色路线只作提示，不会自动转向。点城市地图编号或订单选择下一单。</p></div>
 <div><b>01 / 一单一箱</b><p>每个地址只认对应编号的实体包裹。车里共 12 个订单箱，六种类型。看箱体门牌号或点顶部「订单」确认；选一单会同步团队导航。</p></div>
 <div><b>02 / 两个人都能开车</b><p>房主起步驾驶，队友在货箱。先停车，驾驶员按 V 离座；另一人走到前方，按 E 接管空座。驾驶位只能坐一人。两人都离座时砖头只管油门，不会转向。</p></div>
 <div><b>03 / 认号、拿起、投出去</b><p>WASD / 左下移动区走动，拖动右侧画面观察。低头看编号箱，E /「拿起」。走到后沿，G / 门钮开后门，F /「瞄准」辅助锁定门窗；按住空格 / 左键 /「投掷」蓄力，松手扔出。R 放下。</p></div>
@@ -147,7 +153,7 @@
 </div><p class="fineprint">到后门后点「下车」可走到街上；先开门并减速至 12 km/h 以下。下车后可回收地上快递，空手靠近车头维修。货箱为轻量碰撞 / 惯性物理，不是完整刚体或软体模拟。砖头驾驶仅为游戏设定。</p><button id="closeHelpBtn" class="primary">明白了 →</button></div></section>
  <section id="endScreen" class="screen modalScreen hidden"><div class="modal"><div class="eyebrow">SHIFT COMPLETE</div><h2>这趟，送完了。</h2><div class="endMark">✓</div><div class="endStats"><div><small>收入</small><strong id="endScore">$0</strong></div><div><small>用时</small><strong id="endTime">00:00</strong></div><div><small>掉落货物</small><strong id="endLost">0</strong></div></div><p id="endBest"></p><button id="againBtn" class="primary">再跑一轮 →</button><button id="backMenuBtn" class="secondary">返回菜单</button></div></section>
  <section id="errorScreen" class="screen modalScreen hidden"><div class="modal"><h2>画面没有启动</h2><p id="errorText">请使用支持 WebGL 的浏览器打开。</p><button id="reloadBtn" class="primary">重新加载</button></div></section>
-<section id="wardrobe" class="screen wardrobe hidden"><header><div class="eyebrow">CREW FILE / 实时 3D 模型</div><h2>搭档上岗<span>V0.7 ONLINE</span></h2><p>拖动画面旋转 · 选择动作实时预览</p></header><button id="closeWardrobe" class="control galleryClose">返回菜单 ×</button><div class="avatarCaption"><small id="crewNumber">COURIER / 01</small><strong id="crewName">橙帽快递员</strong><span id="poseTitle">待机呼吸</span></div><div class="poseDock"><div class="crewSelect"><button id="crew0" class="active">01 / 橙帽</button><button id="crew1">02 / 蓝帽</button><span>关节动画 · 无贴图依赖</span></div><div class="poseGrid"><button data-pose="idle">待机</button><button data-pose="walk">走路</button><button data-pose="carry">抱箱走</button><button data-pose="charge">蓄力</button><button data-pose="throw">投掷</button><button data-pose="interact">开门</button><button data-pose="drive">驾驶</button><button data-pose="wave">挥手</button></div></div></section>
+<section id="wardrobe" class="screen wardrobe hidden"><header><div class="eyebrow">CREW FILE / 实时 3D 模型</div><h2>搭档上岗<span>V0.7.1 ONLINE</span></h2><p>拖动画面旋转 · 选择动作实时预览</p></header><button id="closeWardrobe" class="control galleryClose">返回菜单 ×</button><div class="avatarCaption"><small id="crewNumber">COURIER / 01</small><strong id="crewName">橙帽快递员</strong><span id="poseTitle">待机呼吸</span></div><div class="poseDock"><div class="crewSelect"><button id="crew0" class="active">01 / 橙帽</button><button id="crew1">02 / 蓝帽</button><span>关节动画 · 无贴图依赖</span></div><div class="poseGrid"><button data-pose="idle">待机</button><button data-pose="walk">走路</button><button data-pose="carry">抱箱走</button><button data-pose="charge">蓄力</button><button data-pose="throw">投掷</button><button data-pose="interact">开门</button><button data-pose="drive">驾驶</button><button data-pose="wave">挥手</button></div></div></section>
  <noscript><div class="noscript">游戏需要 JavaScript，请使用浏览器打开，而不是文件预览器。</div></noscript>
 </main>
 <script>
@@ -267,7 +273,20 @@
 const parcelType=k=>PACKAGE_TYPES[k]||PACKAGE_TYPES[0];
 let adaptiveScale=2, fpsEMA=60, slowTime=0, fastTime=0, qualityWait=0;
 const maxViewport=renderer.gl.getParameter(renderer.gl.MAX_VIEWPORT_DIMS);
-function save(){try{localStorage.setItem(SAVE_KEY,JSON.stringify({version:'0.7.0-online',best,credits,upgrades,sound:soundOn,assist,quality,motion,nickname}));}catch(e){}}
+function save(){try{localStorage.setItem(SAVE_KEY,JSON.stringify({version:'0.7.1-online',best,credits,upgrades,sound:soundOn,assist,quality,motion,nickname}));}catch(e){}}
+const NET_SETTINGS_KEY='crash_delivery_network_v071';
+let netSettings={policy:'auto',turnUrl:'',turnUser:'',turnPass:'',extraStun:''};
+try{const n=JSON.parse(localStorage.getItem(NET_SETTINGS_KEY)||'{}');if(n&&typeof n==='object')Object.assign(netSettings,n);}catch(e){}
+function cleanIceUrl(v,kind){v=String(v||'').trim();if(!v)return '';const ok=kind==='turn'?/^(turns?):[^\\s]+$/i:/^stuns?:[^\\s]+$/i;return ok.test(v)?v:'';}
+function saveNetSettings(){try{localStorage.setItem(NET_SETTINGS_KEY,JSON.stringify(netSettings));}catch(e){}}
+function hasTurn(){return !!cleanIceUrl(netSettings.turnUrl,'turn');}
+function defaultStun(){return ['stun:stun.l.google.com:19302'];}
+function iceServersFromSettings(){const servers=[];const stun=[...defaultStun()];const extra=cleanIceUrl(netSettings.extraStun,'stun');if(extra&&!stun.includes(extra))stun.push(extra);if(netSettings.policy!=='relay')for(const urls of stun)servers.push({urls});const turn=cleanIceUrl(netSettings.turnUrl,'turn');if(turn&&netSettings.policy!=='direct'){const t={urls:turn};if(netSettings.turnUser)t.username=String(netSettings.turnUser);if(netSettings.turnPass)t.credential=String(netSettings.turnPass);servers.push(t);}return servers;}
+function rtcConfigFromSettings(){const policy=netSettings.policy==='relay'&&hasTurn()?'relay':'all';return {iceServers:iceServersFromSettings(),iceTransportPolicy:policy,bundlePolicy:'balanced'};}
+function syncNetworkForm(){const map={netPolicy:netSettings.policy,turnUrl:netSettings.turnUrl,turnUser:netSettings.turnUser,turnPass:netSettings.turnPass,extraStun:netSettings.extraStun};for(const [id,v] of Object.entries(map)){const el=$(id);if(el)el.value=v||'';}const b=$('networkBtn');if(b)b.innerHTML=hasTurn()?'🌐 网络设置 <strong>TURN ✓</strong>':'🌐 网络设置';}
+function readNetworkForm(){const policy=$('netPolicy')?.value||'auto',turnUrl=cleanIceUrl($('turnUrl')?.value,'turn'),extraStun=cleanIceUrl($('extraStun')?.value,'stun');if(policy==='relay'&&!turnUrl)throw Error('强制中继需要填写有效 TURN 地址。');netSettings={policy:['auto','direct','relay'].includes(policy)?policy:'auto',turnUrl,turnUser:String($('turnUser')?.value||'').trim(),turnPass:String($('turnPass')?.value||''),extraStun};}
+function setNetStatus(text,state='idle'){net.status=text;const el=$('netStatus');if(el){el.textContent=text;el.dataset.state=state;}}
+
 let seed=981;
 function rnd(){seed=(Math.imul(seed,1664525)+1013904223)|0;return(seed>>>0)/4294967296;}
 const state={mode:'menu',view:'drive',time:0,score:0,delivered:0,shots:0,lost:0,explosions:0,combo:0,lastDelivery:-100,brick:false,charge:0,charging:false,chargeSource:null,cooldown:0,toastUntil:0,shake:0,hitCooldown:0,hintUntil:0,switchUntil:0,warnUntil:0,helpFrom:'menu',nextId:1,damageCost:0,trickCount:0,lastBreakdown:''};
@@ -281,18 +300,18 @@
 const houses=[], packages=[], cargo=[], debris=[], traffic=[], lights=[], effects=[];
 // Multiplayer 0.2: one authoritative world, two independent couriers.
 // Rendering/prediction never owns parcel inventory, doors, scores or vehicle physics.
-const NET_PROTOCOL='crash-delivery-mp070-1';
+const NET_PROTOCOL='crash-delivery-mp071-1';
 const net={mode:'solo',peer:null,conn:null,room:'',connected:false,executing:false,applying:false,
  remoteActor:null,remoteInput:null,remoteState:null,hostActor:null,lastInput:0,lastRecv:0,lastSend:0,
  actionSeq:0,ack:0,lastAction:0,epoch:0,seenEpoch:-1,snapshot:null,receivedAt:0,watch:false,
- rtt:0,lastPing:0,token:0,timer:null,status:'创建房间，或输入好友的 6 位房间码。',pauseWho:'',pendingThrow:null,remoteName:'队友'};
+ rtt:0,lastPing:0,token:0,timer:null,status:'创建房间，或输入好友的 6 位房间码。',pauseWho:'',pendingThrow:null,remoteName:'队友',linkType:'',iceState:'idle',candidateTypes:new Set(),remoteCandidateTypes:new Set(),everConnected:false,reconnecting:false,reconnectAttempt:0,reconnectTimer:null,intentionalClose:false,pc:null,lastStats:0,selectedPair:'',relay:false,worldSeq:0,lastWorldSeq:-1};
 const PERSONAL_FIELDS=['view','charge','charging','chargeSource','cooldown','switchUntil'];
 const blankInput=()=>({keys:new Set(),left:new Set(),right:new Set(),gas:new Set(),brake:new Set(),forward:new Set(),back:new Set(),walkLeft:new Set(),walkRight:new Set(),stickX:0,stickY:0});
 const newCourier=id=>({id,name:id?'蓝帽快递员':'橙帽快递员',p:[id? .42:-.42,2.38,-.9],yaw:Math.PI,pitch:-.20,held:null,walkPhase:0,move:0,aimGate:null,aimHouse:null,focus:null,anim:'idle',animAt:0});
 const newPersonal=()=>({view:'cargo',charge:0,charging:false,chargeSource:null,cooldown:0,switchUntil:0});
 function remoteReady(){return net.mode==='host'&&net.connected&&net.remoteActor;}
 function networked(){return net.mode!=='solo';}
-function netText(text){net.status=text;const el=$('netStatus');if(el)el.textContent=text;}
+function netText(text,state){setNetStatus(text,state||'idle');}
 function netSend(m,optional=false){if(!net.connected||!net.conn?.open)return false;try{if(optional&&(net.conn.bufferSize>1||(net.conn.dataChannel?.bufferedAmount||0)>65536))return false;net.conn.send(m);return true;}catch(e){return false;}}
 function inRemote(fn){if(!net.remoteActor)return;const oldPlayer=player,oldInput=inputs,oldKind=currentKind,oldExecuting=net.executing,personal={};net.localView=state.view;
  for(const k of PERSONAL_FIELDS){personal[k]=state[k];state[k]=net.remoteState[k];}player=net.remoteActor;inputs=net.remoteInput;net.executing=true;
@@ -301,45 +320,55 @@
 function localAvatar(){return {...player,name:cleanNickname(player.name||nickname),view:state.view,charge:state.charge,charging:state.charging};}
 function otherAvatar(){if(net.mode==='host'&&net.remoteActor)return {...net.remoteActor,...net.remoteState};if(net.mode==='guest')return net.hostActor;return null;}
 function hasDriver(){if(state.view==='drive')return true;return otherAvatar()?.view==='drive';}
-function roomId(code){return 'crash-delivery-mp070-'+code;}
+function roomId(code){return 'crash-delivery-mp071-'+code;}
 function makeRoomCode(){const a=new Uint32Array(1);crypto.getRandomValues(a);return String(100000+a[0]%900000);}
 let peerLoading=null;
 function loadPeer(){if(window.Peer)return Promise.resolve();if(peerLoading)return peerLoading;
  peerLoading=new Promise(async(resolve,reject)=>{for(const src of ['https://cdn.jsdelivr.net/npm/peerjs@1.5.5/dist/peerjs.min.js','https://unpkg.com/peerjs@1.5.5/dist/peerjs.min.js']){
    try{await new Promise((ok,no)=>{const s=document.createElement('script');let done=false;const finish=e=>{if(done)return;done=true;clearTimeout(t);e?(s.remove(),no(e)):ok();};const t=setTimeout(()=>finish(Error('加载超时')),7000);s.src=src;s.async=true;s.onload=()=>finish(window.Peer?null:Error('组件不可用'));s.onerror=()=>finish(Error('下载失败'));document.head.appendChild(s);});resolve();return;}catch(e){}
  }reject(Error('联机组件下载失败。单人及角色预览仍可用；请检查网络后重试。'));}).finally(()=>peerLoading=null);return peerLoading;}
-function peerOptions(){// Optional own signaling/TURN settings; no private credentials are bundled.
- const custom=window.CRASH_NETWORK_CONFIG;return custom&&typeof custom==='object'?custom:{debug:0};}
-function stopNetwork(toMenu=true){net.token++;clearTimeout(net.timer);net.timer=null;const c=net.conn,p=net.peer;net.connected=false;net.conn=null;net.peer=null;net.mode='solo';net.remoteActor=net.remoteInput=net.remoteState=net.hostActor=null;net.snapshot=null;net.watch=false;net.pendingThrow=null;net.remoteName='队友';
- try{c?.close();p?.destroy();}catch(e){}net.room='';net.pauseWho='';if(toMenu)resetGame(true);$('hostBtn').disabled=$('joinBtn').disabled=false;netText('创建房间，或输入好友的 6 位房间码。');updateNetHUD();}
-function roomFailure(text){clearTimeout(net.timer);net.timer=null;netText(text);$('hostBtn').disabled=$('joinBtn').disabled=false;}
+function peerOptions(){// Explicit STUN/TURN. window.CRASH_NETWORK_CONFIG may override PeerJS signaling fields without exposing game state.
+ const custom=window.CRASH_NETWORK_CONFIG&&typeof window.CRASH_NETWORK_CONFIG==='object'?window.CRASH_NETWORK_CONFIG:{};
+ const cfg={...rtcConfigFromSettings(),...(custom.config||{})};if(!cfg.iceServers)cfg.iceServers=iceServersFromSettings();
+ return {...custom,debug:custom.debug??0,config:cfg};}
+function getPeerConnection(c=net.conn){return c?.peerConnection||c?._negotiator?.peerConnection||c?.dataChannel?._pc||null;}
+function candidateTypeFromString(c){const m=String(c||'').match(/\\btyp\\s+(host|srflx|prflx|relay)\\b/i);return m?m[1].toLowerCase():'';}
+function describeLinkType(localType='',remoteType=''){if(localType==='relay'||remoteType==='relay')return 'TURN 中继';if(['srflx','prflx','host'].includes(localType)||['srflx','prflx','host'].includes(remoteType))return 'P2P 直连';return net.connected?'已连接':'协商中';}
+async function updateRtcStats(force=false){if(!net.connected)return;const now=performance.now();if(!force&&now-net.lastStats<900)return;net.lastStats=now;const pc=getPeerConnection();if(!pc?.getStats)return;try{const stats=await pc.getStats();let pair=null,local=null,remote=null;stats.forEach(r=>{if(r.type==='transport'&&r.selectedCandidatePairId)pair=stats.get(r.selectedCandidatePairId)||pair;if(r.type==='candidate-pair'&&r.state==='succeeded'&&r.nominated)pair=r;});if(pair){local=stats.get(pair.localCandidateId);remote=stats.get(pair.remoteCandidateId);const lt=local?.candidateType||'',rt=remote?.candidateType||'';net.linkType=describeLinkType(lt,rt);net.relay=lt==='relay'||rt==='relay';net.selectedPair=[lt,rt].filter(Boolean).join('↔');if(Number.isFinite(pair.currentRoundTripTime))net.rtt=Math.max(0,Math.round(pair.currentRoundTripTime*1000));}}catch(e){}}
+function bindRtcDiagnostics(c){setTimeout(()=>{if(c!==net.conn)return;const pc=getPeerConnection(c);if(!pc)return;net.pc=pc;const scan=()=>{if(c!==net.conn)return;net.iceState=pc.iceConnectionState||pc.connectionState||'checking';if(net.connected)updateRtcStats(true);updateNetHUD();};pc.addEventListener?.('iceconnectionstatechange',scan);pc.addEventListener?.('connectionstatechange',scan);pc.addEventListener?.('icecandidate',e=>{const t=candidateTypeFromString(e.candidate?.candidate);if(t)net.candidateTypes.add(t);});scan();},0);}
+function restoreConnection(){clearTimeout(net.timer);net.connected=true;net.reconnecting=false;net.reconnectAttempt=0;net.lastInput=net.lastRecv=performance.now();net.lastSend=0;net.pauseWho=state.mode==='paused'?'网络已恢复 · 点继续派送':'';if(net.mode==='host'){netSend({t:'hello',protocol:NET_PROTOCOL});netSend(netSnapshot());}else netSend({t:'identity',nickname});netText('已重新连接 · 正在同步世界…','ok');syncButtons();updateNetHUD();}
+function scheduleGuestReconnect(){if(net.intentionalClose||net.mode!=='guest'||!net.room)return;clearTimeout(net.reconnectTimer);if(net.reconnectAttempt>=4){netText('自动重连失败。请返回菜单重新加入；若跨网络失败，请配置 TURN。','bad');return;}const delay=[900,1800,3500,6500][net.reconnectAttempt++]||6500;net.reconnecting=true;netText('连接中断 · '+(delay/1000).toFixed(1)+' 秒后自动重连（'+net.reconnectAttempt+'/4）','warn');net.reconnectTimer=setTimeout(reconnectGuest,delay);}
+async function reconnectGuest(){if(net.intentionalClose||net.mode!=='guest'||net.connected)return;try{await loadPeer();let p=net.peer;if(!p||p.destroyed){p=net.peer=new Peer(undefined,peerOptions());await new Promise((ok,no)=>{const t=setTimeout(()=>no(Error('信令连接超时')),7000);p.once('open',()=>{clearTimeout(t);ok();});p.once('error',e=>{clearTimeout(t);no(e);});});}else if(!p.open){try{p.reconnect();}catch(e){}await new Promise((ok,no)=>{const t=setTimeout(()=>no(Error('信令重连超时')),5000);p.once('open',()=>{clearTimeout(t);ok();});});}if(net.intentionalClose||net.connected)return;const c=p.connect(roomId(net.room),{reliable:true,serialization:'json',metadata:{protocol:NET_PROTOCOL,nickname,reconnect:true,turn:hasTurn(),policy:netSettings.policy}});setupConn(c);}catch(e){scheduleGuestReconnect();}}
+function stopNetwork(toMenu=true){net.intentionalClose=true;net.token++;clearTimeout(net.timer);clearTimeout(net.reconnectTimer);net.timer=net.reconnectTimer=null;const c=net.conn,p=net.peer;net.connected=false;net.conn=null;net.peer=null;net.pc=null;net.mode='solo';net.remoteActor=net.remoteInput=net.remoteState=net.hostActor=null;net.snapshot=null;net.watch=false;net.pendingThrow=null;net.remoteName='队友';net.everConnected=false;net.reconnecting=false;net.reconnectAttempt=0;net.linkType='';net.iceState='idle';net.candidateTypes.clear();net.remoteCandidateTypes.clear();
+ try{c?.close();p?.destroy();}catch(e){}net.intentionalClose=false;net.room='';net.pauseWho='';if(toMenu)resetGame(true);$('hostBtn').disabled=$('joinBtn').disabled=false;netText('创建房间，或输入好友的 6 位房间码。');updateNetHUD();}
+function roomFailure(text){clearTimeout(net.timer);net.timer=null;netText(text,'bad');$('hostBtn').disabled=$('joinBtn').disabled=false;}
 function describePeerError(e){return ({'unavailable-id':'房间码已占用，请重新创建。','peer-unavailable':'没有找到房间，请核对房间码和双方版本。','network':'无法连接信令服务，请检查网络。','browser-incompatible':'浏览器不支持此联机方式，请换用新版浏览器。','webrtc':'设备之间未能建立直连，请尝试另一网络。','server-error':'信令服务暂时不可达。'}[e?.type]||('连接错误：'+(e?.type||e?.message||'未知')));}
-async function hostRoom(){nickname=cleanNickname($('nicknameInput')?.value||nickname);player.name=nickname;save();stopNetwork(false);const token=net.token;net.mode='host';net.room=makeRoomCode();$('hostBtn').disabled=$('joinBtn').disabled=true;netText('正在连接房间服务…');
+async function hostRoom(){nickname=cleanNickname($('nicknameInput')?.value||nickname);player.name=nickname;save();stopNetwork(false);net.intentionalClose=false;const token=net.token;net.mode='host';net.room=makeRoomCode();$('hostBtn').disabled=$('joinBtn').disabled=true;netText('① 正在连接房间信令…','working');
  try{await loadPeer();if(token!==net.token)return;const p=net.peer=new Peer(roomId(net.room),peerOptions());
- p.on('open',()=>{if(token!==net.token)return;clearTimeout(net.timer);netText('房间码 '+net.room+' · 等待队友加入 · 双方都需使用V0.7 ONLINE');$('copyRoomBtn').classList.remove('hidden');});
- p.on('connection',c=>{if(token!==net.token)return c.close();if(net.conn||c.metadata?.protocol!==NET_PROTOCOL){c.on('open',()=>{try{c.send({t:'reject',reason:net.conn?'房间已满（最多 2 人）。':'版本不一致，请双方更新到V0.7 ONLINE。'});}catch(e){}setTimeout(()=>c.close(),150);});return;}net.remoteName=cleanNickname(c.metadata?.nickname||'队友');setupConn(c);});
+ p.on('open',()=>{if(token!==net.token)return;clearTimeout(net.timer);netText('房间码 '+net.room+' · 等待队友加入 · '+(hasTurn()?'TURN 已配置':'P2P / STUN'),'ok');$('copyRoomBtn').classList.remove('hidden');});
+ p.on('connection',c=>{if(token!==net.token)return c.close();if(net.conn||c.metadata?.protocol!==NET_PROTOCOL){c.on('open',()=>{try{c.send({t:'reject',reason:net.conn?'房间已满（最多 2 人）。':'版本不一致，请双方更新到 V0.7.1 ONLINE。'});}catch(e){}setTimeout(()=>c.close(),150);});return;}net.remoteName=cleanNickname(c.metadata?.nickname||'队友');setupConn(c);});
  p.on('error',e=>roomFailure(describePeerError(e)));p.on('disconnected',()=>{if(token===net.token&&!p.destroyed){netText('房间信令暂时断开；已有直连会继续，正在重连。');try{p.reconnect();}catch(e){}}});
  net.timer=setTimeout(()=>{if(!p.open)roomFailure('房间服务连接超时，请检查网络后重试。');},14000);
  }catch(e){if(token===net.token)roomFailure(e.message);}}
-async function joinRoom(){nickname=cleanNickname($('nicknameInput')?.value||nickname);player.name=nickname;save();const code=$('roomCode').value.replace(/\D/g,'').slice(0,6);if(code.length!==6){netText('请输入完整的 6 位房间码。');return;}stopNetwork(false);net.mode='guest';net.room=code;const token=net.token;$('hostBtn').disabled=$('joinBtn').disabled=true;netText('正在加入 '+code+'…');
- try{await loadPeer();if(token!==net.token)return;const p=net.peer=new Peer(undefined,peerOptions());p.on('open',()=>{if(token!==net.token)return;setupConn(p.connect(roomId(code),{reliable:true,serialization:'json',metadata:{protocol:NET_PROTOCOL,nickname}}));});p.on('error',e=>roomFailure(describePeerError(e)));
- net.timer=setTimeout(()=>{if(!net.connected)roomFailure('未能建立直连。请核对版本和房间码，或换一个网络；部分网络需要 TURN 中继。');},18000);
+async function joinRoom(){nickname=cleanNickname($('nicknameInput')?.value||nickname);player.name=nickname;save();const code=$('roomCode').value.replace(/\D/g,'').slice(0,6);if(code.length!==6){netText('请输入完整的 6 位房间码。');return;}stopNetwork(false);net.intentionalClose=false;net.mode='guest';net.room=code;const token=net.token;$('hostBtn').disabled=$('joinBtn').disabled=true;netText('① 正在加入 '+code+' · 连接信令…','working');
+ try{await loadPeer();if(token!==net.token)return;const p=net.peer=new Peer(undefined,peerOptions());p.on('open',()=>{if(token!==net.token)return;setupConn(p.connect(roomId(code),{reliable:true,serialization:'json',metadata:{protocol:NET_PROTOCOL,nickname,turn:hasTurn(),policy:netSettings.policy}}));});p.on('error',e=>roomFailure(describePeerError(e)));
+ net.timer=setTimeout(()=>{if(!net.connected)roomFailure(hasTurn()?'未能建立 WebRTC。TURN 已配置，请先运行 ICE 自检确认 relay candidate。':'未能建立 P2P。请在「网络设置」配置 TURN 中继后重试。');},18000);
  }catch(e){if(token===net.token)roomFailure(e.message);}}
-function beginConnection(){clearTimeout(net.timer);net.connected=true;net.lastInput=net.lastRecv=performance.now();net.lastAction=net.actionSeq=net.ack=0;net.lastSend=0;net.pendingThrow=null;net.watch=false;net.snapshot=null;net.seenEpoch=-1;
+function beginConnection(){clearTimeout(net.timer);net.connected=true;net.everConnected=true;net.reconnecting=false;net.reconnectAttempt=0;net.intentionalClose=false;net.lastInput=net.lastRecv=performance.now();net.lastAction=net.actionSeq=net.ack=0;net.lastSend=0;net.pendingThrow=null;net.watch=false;net.snapshot=null;net.seenEpoch=-1;
  net.applying=true;resetGame(false);net.applying=false;
  if(net.mode==='host'){player.id=0;player.name=nickname;net.remoteActor=newCourier(1);net.remoteActor.name=net.remoteName;net.remoteInput=blankInput();net.remoteState=newPersonal();net.epoch++;netSend({t:'hello',protocol:NET_PROTOCOL});netSend(netSnapshot());showHint('你负责驾驶，队友在货箱内独立操作。T /「看队友」切换车内观察。',8);}
  else{player.id=1;player.name=nickname;state.view='cargo';player.p=[.42,2.38,-.9];net.hostActor={...newCourier(0),view:'drive'};showHint('你是 2 号快递员。走到车尾、打开后门、拿箱并投掷。',8);}
- if(net.mode==='guest')netSend({t:'identity',nickname});netText('房间 '+net.room+' · 双人已连接');syncButtons();updateNetHUD();}
-function setupConn(c){net.conn=c;c.on('open',()=>{if(c===net.conn)beginConnection();});c.on('data',m=>{if(c===net.conn)onNetData(m);});c.on('close',()=>{if(c===net.conn)connectionLost();});c.on('error',()=>{if(c===net.conn)connectionLost();});}
-function connectionLost(){net.connected=false;net.conn=null;netText('队友连接已断开。可退出联机后重新创建 / 加入。');net.pauseWho='连接已断开';
+ if(net.mode==='guest')netSend({t:'identity',nickname});netText('② WebRTC 已连接 · 正在确认链路…','ok');setTimeout(()=>updateRtcStats(true),350);syncButtons();updateNetHUD();}
+function setupConn(c){net.conn=c;netText(net.reconnecting?'正在重新协商 ICE…':'② 正在协商 ICE / NAT 穿透…','working');bindRtcDiagnostics(c);c.on('open',()=>{if(c!==net.conn)return;if(net.everConnected&&state.mode!=='menu')restoreConnection();else beginConnection();});c.on('data',m=>{if(c===net.conn)onNetData(m);});c.on('close',()=>{if(c===net.conn)connectionLost();});c.on('error',()=>{if(c===net.conn)connectionLost();});}
+function connectionLost(){if(net.intentionalClose)return;net.connected=false;net.conn=null;net.pc=null;net.linkType='';netText(net.mode==='guest'?'队友连接中断 · 正在准备自动重连…':'队友连接中断 · 房间仍保留，等待对方重连…','warn');net.pauseWho='网络断线保护';
  if(net.mode==='host'&&net.remoteActor?.held){const c=net.remoteActor.held;c.p=[.6,1.4,-.4];c.v=[0,0,0];cargo.push(c);net.remoteActor.held=null;}
- if(net.remoteInput)net.remoteInput=blankInput();if(state.mode==='playing'){net.applying=true;pause();net.applying=false;}updateNetHUD();}
+ if(net.remoteInput)net.remoteInput=blankInput();if(state.mode==='playing'){net.applying=true;pause();net.applying=false;}if(net.mode==='guest')scheduleGuestReconnect();updateNetHUD();}
 const q3=x=>Math.round(x*1000)/1000;
 const qvec=p=>p.map(q3);
 function packBox(c){return {id:c.id,kind:c.kind,p:qvec(c.p),rot:qvec(c.rot),v:qvec(c.v||[0,0,0]),delivered:!!c.delivered,order:c.order||0,lossCounted:!!c.lossCounted,wrongAt:c.wrongAt||[],integrity:q3(c.integrity??100),bounces:c.bounces||0,blastBoosted:!!c.blastBoosted,mystery:c.mystery||null};}
 function unpackBox(c){return {id:c.id,kind:c.kind,r:parcelType(c.kind).r,mass:parcelType(c.kind).mass,p:c.p.slice(),rot:c.rot.slice(),v:(c.v||[0,0,0]).slice(),spin:[0,0,0],age:0,thrown:false,pending:null,delivered:!!c.delivered,order:c.order||0,lossCounted:!!c.lossCounted,wrongAt:c.wrongAt||[],lastHit:-2,dist:0,vehicleSpeed:0,integrity:c.integrity??100,bounces:c.bounces||0,blastBoosted:!!c.blastBoosted,mystery:c.mystery||null};}
 function packActor(a,s){return {id:a.id,name:cleanNickname(a.name||('快递员'+(a.id+1))),p:qvec(a.p),yaw:q3(a.yaw),pitch:q3(a.pitch),walkPhase:q3(a.walkPhase),move:q3(a.move||0),anim:a.anim,animAt:q3(a.animAt||0),held:a.held?packBox(a.held):null,repair:a.repair?{at:a.repair.at}:null,view:s.view,charge:q3(s.charge||0),charging:!!s.charging};}
-function netSnapshot(){return {t:'world',protocol:NET_PROTOCOL,epoch:net.epoch,ack:net.lastAction,mode:state.mode,pauseWho:net.pauseWho,
+function netSnapshot(){return {t:'world',protocol:NET_PROTOCOL,epoch:net.epoch,seq:++net.worldSeq,ack:net.lastAction,mode:state.mode,pauseWho:net.pauseWho,
  car:{p:qvec(car.p),yaw:q3(car.yaw),speed:q3(car.speed),hp:q3(car.hp),steer:q3(car.steer),roll:q3(car.roll),wheelAngle:q3(car.wheelAngle),velocity:qvec(car.velocity),fault:car.fault||''},
  upgrades:{...upgrades},state:{time:q3(state.time),score:state.score,delivered:state.delivered,brick:state.brick,shots:state.shots,lost:state.lost,explosions:state.explosions,damageCost:state.damageCost,trickCount:state.trickCount,selectedOrder:state.selectedOrder||0,recovered:state.recovered||0,repairs:state.repairs||0,wrong:state.wrong||0,lastBreakdown:state.lastBreakdown||''},
  doors:doors.map(d=>({angle:q3(d.angle),target:d.target})),brick:{p:qvec(brickBody.p),rot:qvec(brickBody.rot),placed:brickBody.placed},
@@ -353,10 +382,11 @@
  for(const k of ['stickX','stickY'])net.remoteInput[k]=Number.isFinite(i[k])?clamp(i[k],-1,1):0;
  if(Number.isFinite(i.yaw))net.remoteActor.yaw=wrapAngle(i.yaw);if(Number.isFinite(i.pitch))net.remoteActor.pitch=clamp(i.pitch,-1.28,1.02);net.lastInput=performance.now();}
 function action(name,args={}){if(net.mode!=='guest'||!net.connected)return 0;const seq=++net.actionSeq;netSend({t:'action',name,args,seq,epoch:net.seenEpoch,input:movementInput()});return seq;}
+function sendInputNow(){if(net.mode==='guest'&&net.connected)netSend({t:'input',input:movementInput()});}
 function worldEvent(kind,data){if(net.mode==='host'&&net.connected&&!net.applying)netSend({t:'event',kind,...data});}
 function onNetData(m){if(!m||typeof m!=='object')return;net.lastRecv=performance.now();
  if(m.t==='ping'){netSend({t:'pong',at:m.at});return;}if(m.t==='pong'){net.rtt=Math.max(0,Math.round(performance.now()-m.at));return;}
- if(m.t==='hello'&&m.protocol!==NET_PROTOCOL){roomFailure('版本不一致，请双方更新到V0.7 ONLINE。');return;}
+ if(m.t==='hello'&&m.protocol!==NET_PROTOCOL){roomFailure('版本不一致，请双方更新到 V0.7.1 ONLINE。');return;}
  if(m.t==='reject'){roomFailure(m.reason||'无法加入房间。');return;}
  if(net.mode==='host'){
   if(m.t==='identity'){net.remoteName=cleanNickname(m.nickname);if(net.remoteActor)net.remoteActor.name=net.remoteName;return;}
@@ -366,7 +396,7 @@
   const a=m.args&&typeof m.args==='object'?m.args:{};
   inRemote(()=>{switch(m.name){case 'interact':interact();break;case 'door':if(a.index===0||a.index===1)toggleDoor(a.index);break;case 'doors':toggleBothDoors();break;case 'drop':dropPackage();break;
   case 'charge':startCharge('remote');break;case 'throw':if(state.charging&&player.held){state.charge=clamp(Number.isFinite(a.power)?a.power:.1,.1,Math.min(1,state.charge+.20));throwPackage();cancelCharge();}break;
-  case 'mobility':mobility();break;case 'selectOrder':selectOrder(a.num);break;case 'cancel':cancelCharge();break;case 'view':if(['cargo','drive'].includes(a.view))setView(a.view);break;case 'brick':toggleBrick();break;case 'wave':pulse('wave');break;}});syncButtons();return;
+  case 'mobility':mobility();break;case 'selectOrder':selectOrder(a.num);break;case 'cancel':cancelCharge();break;case 'view':if(['cargo','drive'].includes(a.view))setView(a.view);break;case 'brick':toggleBrick();break;case 'wave':pulse('wave');break;}});syncButtons();netSend(netSnapshot());return;
  }
  if(net.mode!=='guest')return;
  if(m.t==='world'&&m.protocol===NET_PROTOCOL){applyHostSnapshot(m);return;}
@@ -376,7 +406,7 @@
  }finally{net.applying=false;}}
 }
 function applyHostSnapshot(m){if(!m.car||!Array.isArray(m.actors)||!m.actors[1])return;
- if(!Number.isSafeInteger(m.epoch)||m.epoch<net.seenEpoch)return;const fresh=net.seenEpoch!==m.epoch;net.applying=true;try{
+ if(!Number.isSafeInteger(m.epoch)||m.epoch<net.seenEpoch)return;if(Number.isSafeInteger(m.seq)&&m.seq<=net.lastWorldSeq)return;const fresh=net.seenEpoch!==m.epoch;if(fresh)net.lastWorldSeq=-1;if(Number.isSafeInteger(m.seq))net.lastWorldSeq=m.seq;net.applying=true;try{
  if(fresh){resetGame();net.seenEpoch=m.epoch;player.id=1;net.pendingThrow=null;net.hostActor=null;}
  net.snapshot=m;net.receivedAt=performance.now();net.ack=m.ack||0;Object.assign(state,m.state);/* Only host simulates the vehicle; guest's local garage stays private. */net.pauseWho=m.pauseWho||'';
  const a=m.actors[1],changed=state.view!==a.view;if(fresh||changed){state.view=a.view;player.p=a.p.slice();player.yaw=a.yaw;player.pitch=a.pitch;cancelCharge();clearInputs();net.watch=false;cameraBlend=0;}
@@ -410,17 +440,25 @@
  if(state.mode==='playing'){state.cooldown=Math.max(0,state.cooldown-dt);state.shake=Math.max(0,state.shake-dt*.7);if(state.charging)state.charge=Math.min(1,state.charge+dt*.9);stepPlayer(dt);stepEffects(dt);}}
 function netTick(){const now=performance.now();if(!net.connected||!net.conn?.open)return;
  const sendEvery=net.mode==='host'?(coarse()?90:70):50;if(now-net.lastSend>=sendEvery){net.lastSend=now;if(net.mode==='host')netSend(netSnapshot(),true);else netSend({t:'input',input:movementInput()},true);}
- if(now-net.lastPing>1800){net.lastPing=now;netSend({t:'ping',at:now},true);}
+ if(now-net.lastPing>1500){net.lastPing=now;netSend({t:'ping',at:now},true);}
+ if(now-net.lastStats>1100)updateRtcStats();
  if(net.mode==='host'&&now-net.lastInput>700&&net.remoteInput){net.remoteInput.keys.clear();net.remoteInput.stickX=net.remoteInput.stickY=0;}
- if(now-net.lastRecv>6000&&state.mode==='playing'){net.pauseWho='网络等待中，已暂停防止失控';net.applying=true;pause();net.applying=false;}}
+ if(now-net.lastRecv>6500&&state.mode==='playing'){net.pauseWho='网络等待中 · 已暂停防止车辆失控';net.applying=true;pause();net.applying=false;}if(now-net.lastRecv>10000&&net.mode==='guest'&&!net.reconnecting){connectionLost();}}
 function updateNetHUD(){const online=networked(),other=otherAvatar();$('cancelNetBtn').classList.toggle('hidden',!online);$('game').dataset.online=online?'true':'false';$('roomStrip').classList.toggle('hidden',!online);$('leaveBtn').classList.toggle('hidden',!online);$('copyRoomBtn').classList.toggle('hidden',!net.room);
  const myName=cleanNickname(player.name||nickname),otherName=other?cleanNickname(other.name||'队友'):(net.connected?cleanNickname(net.remoteName):'等待队友');const myRole=state.view==='drive'?'驾驶员':'投递员',otherRole=!net.connected?'未连接':other?.view==='drive'?'驾驶员':'投递员';
- const ln=$('localNameHud'),lr=$('localRoleHud'),rn=$('remoteNameHud'),rr=$('remoteRoleHud'),meta=$('roomMetaHud');if(ln)ln.textContent=myName;if(lr)lr.textContent=myRole;if(rn)rn.textContent=otherName;if(rr)rr.textContent=otherRole;if(meta)meta.textContent=(net.room?'#'+net.room:'')+(net.rtt?' · '+net.rtt+'ms':'');
- $('pauseNet').textContent=online?(net.pauseWho||('房间 '+net.room+' · '+myName+' / '+otherName)) :'';
+ const ln=$('localNameHud'),lr=$('localRoleHud'),rn=$('remoteNameHud'),rr=$('remoteRoleHud'),meta=$('roomMetaHud');if(ln)ln.textContent=myName;if(lr)lr.textContent=myRole;if(rn)rn.textContent=otherName;if(rr)rr.textContent=otherRole;if(meta){const link=net.linkType||(!net.connected?'未连接':'ICE');meta.textContent=(net.room?'#'+net.room:'')+' · '+link+(net.rtt?' · '+net.rtt+'ms':'');meta.className='roomMeta '+(net.relay?'relay':'');}
+ $('pauseNet').textContent=online?(net.pauseWho||('房间 '+net.room+' · '+myName+' / '+otherName+' · '+(net.linkType||'ICE 协商中')+(net.rtt?' · '+net.rtt+'ms':''))) :'';
  $('watchBtn').classList.toggle('hidden',!net.connected||state.view!=='drive');$('watchBtn').classList.toggle('active',net.watch);$('watchBtn').textContent=net.watch?'返回路面':'看 '+otherName;
  $('resumeBtn').disabled=networked()&&!net.connected;$('serviceBtn').disabled=$('restartBtn').disabled=net.mode==='guest';
  if(online){$('cargoBtn').querySelector('b').textContent='离开驾驶位';$('autoChip').textContent=state.brick?'砖头压油门':state.view==='cargo'?(hasDriver()?'队友驾驶':'无人掌舵'):'你在驾驶';}}
 async function copyRoom(){if(!net.room)return;const u=new URL(location.href);u.search='';u.hash='';u.searchParams.set('room',net.room);try{await navigator.clipboard.writeText(u.href);netText('邀请链接已复制 · 房间码 '+net.room);}catch(e){netText('房间码 '+net.room+' · 请将此房间码发给好友。');}}
+function openNetworkSettings(){syncNetworkForm();$('networkScreen').classList.remove('hidden');if(state.mode==='playing')pause();}
+function closeNetworkSettings(){$('networkScreen').classList.add('hidden');}
+function formatCandidateTypes(types){const a=[...types];return a.length?a.map(t=>'<span class="'+t+'">'+t+'</span>').join(''):'<span>无候选</span>';}
+async function runIceTest(){const out=$('iceDiag');if(!window.RTCPeerConnection){if(out)out.innerHTML='<b>失败：</b>浏览器没有 RTCPeerConnection。';return;}const temp={...netSettings};try{readNetworkForm();}catch(e){if(out)out.innerHTML='<b>设置错误：</b>'+e.message;return;}if(out)out.innerHTML='正在收集 ICE candidate…';const types=new Set();let pc;try{pc=new RTCPeerConnection(rtcConfigFromSettings());pc.createDataChannel('probe');pc.onicecandidate=e=>{const t=candidateTypeFromString(e.candidate?.candidate);if(t){types.add(t);if(out)out.innerHTML='已发现候选：<div class="iceTypes">'+formatCandidateTypes(types)+'</div>';}};const offer=await pc.createOffer();await pc.setLocalDescription(offer);await new Promise(resolve=>{let done=false;const finish=()=>{if(done)return;done=true;resolve();};pc.onicegatheringstatechange=()=>{if(pc.iceGatheringState==='complete')finish();};setTimeout(finish,6500);});const hasRelay=types.has('relay'),hasSrflx=types.has('srflx')||types.has('prflx');if(out)out.innerHTML='<b>ICE 测试完成：</b><div class="iceTypes">'+formatCandidateTypes(types)+'</div>'+(hasRelay?'<br><b>TURN relay 可用。</b>跨严格 NAT 可走中继。':hasTurn()?'<br><span class="reconnectNote">已配置 TURN，但未得到 relay candidate。请检查地址、账号、密码或防火墙。</span>':'<br>未配置 TURN。')+(hasSrflx?'<br>STUN 已取得公网候选。':'<br>没有 srflx；当前网络可能限制 UDP/STUN。');}catch(e){if(out)out.innerHTML='<b>ICE 测试失败：</b>'+String(e.message||e);}finally{try{pc?.close();}catch(e){}Object.assign(netSettings,temp);syncNetworkForm();}}
+function persistNetworkSettings(){try{readNetworkForm();saveNetSettings();syncNetworkForm();const out=$('iceDiag');if(out)out.innerHTML='已保存。连接策略：<b>'+({auto:'自动 / P2P 优先',direct:'仅直连',relay:'强制 TURN'}[netSettings.policy])+'</b>。'+(hasTurn()?' TURN 已配置。':' 尚未配置 TURN。');}catch(e){const out=$('iceDiag');if(out)out.innerHTML='<b>无法保存：</b>'+e.message;}}
+function clearTurnSettings(){netSettings.turnUrl=netSettings.turnUser=netSettings.turnPass='';if(netSettings.policy==='relay')netSettings.policy='auto';saveNetSettings();syncNetworkForm();const out=$('iceDiag');if(out)out.innerHTML='TURN 凭据已从本机清除。当前将使用 STUN / P2P。';}
+

 let eye=[17,13,87],at=[4,2,68], width=innerWidth,height=innerHeight, simAccumulator=0;
 let driveOrbit=0, driveOrbitGoal=0, currentKind=0, cameraBlend=0, blendEye=null, blendAt=null;
@@ -820,7 +858,7 @@
  Object.assign(car,{p:[3.2,.12,72],yaw:0,speed:0,steer:0,roll:0,wheelAngle:0,hp:100,velocity:[0,0,0],accel:[0,0,0],yawRate:0,kick:[0,0,0],offRoad:0});Object.assign(player,{name:nickname,p:[0,2.38,-.9],yaw:Math.PI,pitch:-.20,held:null,walkPhase:0,aimGate:null,aimHouse:null,focus:null});doors.forEach(d=>{d.angle=d.target=d.vel=0;});Object.assign(brickBody,{p:[.84,.98,-2.47],v:[0,0,0],placed:false,rot:[0,.15,0]});loadCargo();
  resetCityTraffic();
  eye=toMenu?[18,8.8,88]:[16,11,88];at=toMenu?[3.2,1.65,68]:[5,2,65];driveOrbit=driveOrbitGoal=0;cameraBlend=0;simAccumulator=0;
- for(const id of ['pauseScreen','helpScreen','endScreen','garageScreen']){const el=$(id);if(el)el.classList.add('hidden');}$('menu').classList.toggle('hidden',!toMenu);$('hud').classList.toggle('hidden',toMenu);$('toast').classList.remove('show');
+ for(const id of ['pauseScreen','helpScreen','endScreen','garageScreen','networkScreen']){const el=$(id);if(el)el.classList.add('hidden');}$('menu').classList.toggle('hidden',!toMenu);$('hud').classList.toggle('hidden',toMenu);$('toast').classList.remove('show');
  if(!toMenu){initAudio();showHint('先开车靠近目标。B / 🧱 放砖，V /「进货箱」离开驾驶位。',9);world.focus({preventScroll:true});}syncButtons();updateHUD();
 }
 function pause(){if(state.mode!=='playing')return;clearInputs();state.mode='paused';$('pauseScreen').classList.remove('hidden');syncButtons();$('resumeBtn').focus({preventScroll:true});}
@@ -1785,7 +1823,7 @@
 let freezeSimulation=false;
 const pointerStreams=new Map();let joyPointer=null;
 function capture(el,id){try{el.setPointerCapture(id);}catch(e){}}
-function holdControl(id,key,view){const el=$(id);el.addEventListener('pointerdown',e=>{if(state.mode!=='playing'||(state.view!==view&&!(view==='cargo'&&state.view==='outside')))return;e.preventDefault();capture(el,e.pointerId);inputs[key].add(e.pointerId);el.classList.add('pressed');initAudio();});const up=e=>{inputs[key].delete(e.pointerId);if(!inputs[key].size)el.classList.remove('pressed');};for(const type of ['pointerup','pointercancel','lostpointercapture'])el.addEventListener(type,up);}
+function holdControl(id,key,view){const el=$(id);el.addEventListener('pointerdown',e=>{if(state.mode!=='playing'||(state.view!==view&&!(view==='cargo'&&state.view==='outside')))return;e.preventDefault();capture(el,e.pointerId);inputs[key].add(e.pointerId);el.classList.add('pressed');initAudio();sendInputNow();});const up=e=>{inputs[key].delete(e.pointerId);if(!inputs[key].size)el.classList.remove('pressed');sendInputNow();};for(const type of ['pointerup','pointercancel','lostpointercapture'])el.addEventListener(type,up);}
 for(const [id,k] of [['leftBtn','left'],['rightBtn','right'],['gasBtn','gas'],['brakeBtn','brake']])holdControl(id,k,'drive');
 for(const [id,k] of [['walkForward','forward'],['walkBack','back'],['walkLeft','walkLeft'],['walkRight','walkRight']])holdControl(id,k,'cargo');
 world.addEventListener('contextmenu',e=>e.preventDefault());
@@ -1806,9 +1844,9 @@
 window.addEventListener('keydown',e=>{if(!gameKeys.has(e.code)||['INPUT','TEXTAREA','SELECT'].includes(e.target.tagName))return;
  if(e.code==='Escape'||e.code==='KeyP'){if(e.repeat)return;e.preventDefault();if(!$('helpScreen').classList.contains('hidden')){closeHelp();return;}if(state.mode==='playing')pause();else if(state.mode==='paused')resume();return;}
  if(state.mode!=='playing')return;e.preventDefault();inputs.keys.add(e.code);if(e.repeat)return;
- if(e.code==='KeyB')toggleBrick();else if(e.code==='KeyV')(state.view==='outside'||state.view==='cargo'&&player.p[2]>1.45?mobility():setView(state.view==='drive'?'cargo':'drive'));else if(e.code==='KeyE')interact();else if(e.code==='KeyG')toggleBothDoors();else if(e.code==='KeyF')lockTarget();else if(e.code==='KeyR')dropPackage();else if(e.code==='KeyC'){if(state.view==='drive')driveOrbitGoal=0;else{player.yaw=Math.PI;player.pitch=-.15;player.aimGate=player.aimHouse=null;}}else if(e.code==='Space')startCharge('space');
+ if(e.code==='KeyB')toggleBrick();else if(e.code==='KeyV')(state.view==='outside'||state.view==='cargo'&&player.p[2]>1.45?mobility():setView(state.view==='drive'?'cargo':'drive'));else if(e.code==='KeyE')interact();else if(e.code==='KeyG')toggleBothDoors();else if(e.code==='KeyF')lockTarget();else if(e.code==='KeyR')dropPackage();else if(e.code==='KeyC'){if(state.view==='drive')driveOrbitGoal=0;else{player.yaw=Math.PI;player.pitch=-.15;player.aimGate=player.aimHouse=null;}}else if(e.code==='Space')startCharge('space');sendInputNow();
 });
-window.addEventListener('keyup',e=>{inputs.keys.delete(e.code);if(e.code==='Space'){if(state.mode==='playing')e.preventDefault();finishCharge('space');}});
+window.addEventListener('keyup',e=>{inputs.keys.delete(e.code);if(e.code==='Space'){if(state.mode==='playing')e.preventDefault();finishCharge('space');}sendInputNow();});
 $('startBtn').addEventListener('click',()=>{stopNetwork(false);resetGame();});$('hostBtn').addEventListener('click',hostRoom);$('joinBtn').addEventListener('click',joinRoom);$('againBtn').addEventListener('click',()=>resetGame());$('backMenuBtn').addEventListener('click',()=>stopNetwork(true));$('restartBtn').addEventListener('click',()=>resetGame());$('pauseBtn').addEventListener('click',pause);$('resumeBtn').addEventListener('click',resume);$('serviceBtn').addEventListener('click',service);
 $('cargoBtn').addEventListener('click',()=>setView('cargo'));$('brickBtn').addEventListener('click',toggleBrick);$('interactBtn').addEventListener('click',interact);$('leftDoorBtn').addEventListener('click',()=>toggleDoor(0));$('rightDoorBtn').addEventListener('click',()=>toggleDoor(1));$('dropBtn').addEventListener('click',dropPackage);$('assistAimBtn').addEventListener('click',()=>lockTarget());
 $('qualityBtn').addEventListener('click',()=>{quality={auto:'high',high:'low',low:'auto'}[quality];adaptiveScale=2;qualityWait=slowTime=fastTime=0;syncButtons();save();});$('assistBtn').addEventListener('click',()=>{assist=!assist;syncButtons();save();});$('soundBtn').addEventListener('click',()=>{soundOn=!soundOn;initAudio();syncButtons();save();});$('motionBtn').addEventListener('click',()=>{motion=!motion;syncButtons();save();});
@@ -1852,7 +1890,7 @@
  glError:()=>renderer.gl.getError()
 };

-{const el=$('garageBtn');if(el)el.onclick=openGarage;}{const el=$('closeGarageBtn');if(el)el.onclick=closeGarage;}for(const key of ['engine','brakes','grip','mat']){const el=$('upgrade_'+key);if(el)el.onclick=()=>buyUpgrade(key);}$('wardrobeBtn').onclick=openWardrobe;$('closeWardrobe').onclick=closeWardrobe;
+$('networkBtn').onclick=openNetworkSettings;$('pauseNetworkBtn').onclick=openNetworkSettings;$('quickIceBtn').onclick=()=>{openNetworkSettings();runIceTest();};$('closeNetworkBtn').onclick=closeNetworkSettings;$('saveNetworkBtn').onclick=persistNetworkSettings;$('testIceBtn').onclick=runIceTest;$('clearTurnBtn').onclick=clearTurnSettings;{const el=$('garageBtn');if(el)el.onclick=openGarage;}{const el=$('closeGarageBtn');if(el)el.onclick=closeGarage;}for(const key of ['engine','brakes','grip','mat']){const el=$('upgrade_'+key);if(el)el.onclick=()=>buyUpgrade(key);}$('wardrobeBtn').onclick=openWardrobe;$('closeWardrobe').onclick=closeWardrobe;
 $('watchBtn').onclick=toggleWatch;$('waveBtn').onclick=wave;$('copyRoomBtn').onclick=copyRoom;
 $('leaveBtn').onclick=()=>stopNetwork(true);$('cancelNetBtn').onclick=()=>stopNetwork(false);
 for(let id=0;id<2;id++)$('crew'+id).onclick=()=>{preview.which=id;$('crewNumber').textContent='COURIER / 0'+(id+1);$('crewName').textContent=id?'蓝帽快递员':'橙帽快递员';$('crew0').classList.toggle('active',!id);$('crew1').classList.toggle('active',!!id);};
@@ -1865,7 +1903,10 @@
 const inviteCode=new URLSearchParams(location.search).get('room');if(/^\d{6}$/.test(inviteCode||'')){$('roomCode').value=inviteCode;netText('已填入邀请房间 '+inviteCode+'，点击「加入」。');}
 if(window.__deliveryTest)Object.assign(window.__deliveryTest,{
  background:b=>qaBackground=!!b,
- network:()=>({mode:net.mode,connected:net.connected,room:net.room,watch:net.watch,epoch:net.epoch,ack:net.ack,lastAction:net.lastAction,rtt:net.rtt,status:net.status,remote:net.remoteActor?packActor(net.remoteActor,net.remoteState):net.hostActor}),
+ network:()=>({mode:net.mode,connected:net.connected,room:net.room,watch:net.watch,epoch:net.epoch,ack:net.ack,lastAction:net.lastAction,rtt:net.rtt,status:net.status,linkType:net.linkType,iceState:net.iceState,relay:net.relay,reconnecting:net.reconnecting,reconnectAttempt:net.reconnectAttempt,worldSeq:net.worldSeq,lastWorldSeq:net.lastWorldSeq,snapshotDoors:net.snapshot?.doors?.map(d=>({angle:d.angle,target:d.target})),remote:net.remoteActor?packActor(net.remoteActor,net.remoteState):net.hostActor}),
+ networkSettings:()=>({...netSettings,turnPass:netSettings.turnPass?'set':''}),
+ setNetworkSettings:o=>{Object.assign(netSettings,o||{});saveNetSettings();syncNetworkForm();return rtcConfigFromSettings();},
+ rtcConfig:()=>rtcConfigFromSettings(),
  avatar:()=>({local:packActor(player,state),other:otherAvatar()?packActor(otherAvatar(),otherAvatar()):null}),
  gallery:(a='idle',id=0)=>{openWardrobe();preview.which=id;setPreviewAction(a);},
  previewPose:()=>courierPose({...newCourier(0),view:'cargo',move:1,walkPhase:performance.now()/1000*8},performance.now()/1000),
@@ -1878,7 +1919,7 @@
  publish:()=>netSend(netSnapshot())
 });

-extendV06Tests();extendV07Tests();const requiredStartupIds=['game','world','overlay','menu','hud','startBtn','hostBtn','joinBtn'];const missingStartup=requiredStartupIds.filter(id=>!$(id));if(missingStartup.length)console.error('Missing startup DOM:',missingStartup);resize();resetGame(true);const start=$('startBtn');if(start){start.disabled=false;start.textContent='单人测试 →';}window.__deliveryBootReady=true;requestAnimationFrame(frame);
+extendV06Tests();extendV07Tests();const requiredStartupIds=['game','world','overlay','menu','hud','startBtn','hostBtn','joinBtn'];const missingStartup=requiredStartupIds.filter(id=>!$(id));if(missingStartup.length)console.error('Missing startup DOM:',missingStartup);resize();resetGame(true);const start=$('startBtn');if(start){start.disabled=false;start.textContent='单人测试 →';}syncNetworkForm();window.__deliveryBootReady=true;requestAnimationFrame(frame);
 })();
 </script>
 </body></html>
