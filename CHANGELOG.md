# 更新日志

## V0.7.3.1 ONLINE — PLAYER MOVEMENT HOTFIX

- 客机快递员步行改为本地优先预测；房主确认号回收后，从权威状态按未确认输入序列重演移动输入。
- 将客机权威人物状态、预测人物状态与平滑显示位置分开。普通网络误差只调整渲染偏移；岗位/空间转换及严重位置分歧允许硬校正。
- 明确同步货箱内车辆局部坐标与车外世界坐标；进入不同坐标空间时清空插值样本，避免车体世界位移参与货箱人物校正。
- 远端队友使用有界快照缓冲，插值位置、朝向、俯仰、步态相位和移动状态；仅允许最多 160ms 的速度外推。
- 手机摇杆按下、明显改向与松开立即更新本地控制；键盘状态变化立即发送，持续输入仍以 50ms 心跳续传。摇杆松开不会等待房主确认才停下。
- 维持房主对碰撞、包裹、投递、维修、门、角色切换、订单及分数的权威处理；保留 Cloudflare TURN、六路 ICE 诊断与 V0.7.3 车辆预测。
- 新增人物移动网络模拟与测试接口；生产 UI 不显示开发诊断数据。此版本测试使用延迟消息桥，未测真实 TURN RTT。

## V0.7.3 ONLINE — LOW LATENCY

- 客机驾驶加入本地车辆预测，油门、刹车与转向按下/松开立即生效；碰撞、NPC 交通、车辆损伤、维修及任务仍由房主权威计算。
- 驾驶输入新增递增序号、房主确认号与 50ms 输入心跳；拒绝过期输入，断线超时仍自动清除输入。
- 权威位置按误差与实测 RTT 分级平滑收敛；大位移、碰撞损伤、角色切换、重生/状态变化时硬纠正，避免持续橡皮筋。
- 队友角色使用 RTT 自适应缓冲插值，并限制短时外推；低频世界快照不改变关键互动的房主确认流程。
- HUD 明确显示 🟢 P2P / 🟡 TURN / 🔴 未连接及实际测得 RTT，不伪造延迟。
- 六路 TURN 推荐只考虑超时前取得 `typ relay` 的结果；顶部路线标记和最终保存路线使用同一个最佳结果。
- 新增 0/80/150/250/400ms、20ms 抖动、1% 可替换报文丢包测试，并保持旧版本桌面/手机 Chromium 回归。
- 构建输出为独立候选，不覆盖根目录 `index.html`；短期 TURN 凭证继续只留在内存。

## V0.7.2 Hotfix — Network Diagnostics

- 将 Cloudflare 六条 TURN URL 拆成独立 relay-only WebRTC 检测：每条线路分别创建 DataChannel、Offer 并等待 ICE gathering。
- 只把 `typ relay` candidate 记为 TURN 成功；显示每条线路的结果与耗时，区分超时和失败。
- 自动保存本设备实测最快路线并在后续联机配置中优先排列；自动模式仍保留 P2P 优先，强制 TURN 仍可验收。
- 临时 TURN 凭证仅在页面内存中使用；本机只保存选中的公开线路 URL。
- 修复 ICE candidate 类型解析，旧正则无法正确识别 `typ relay`。
- Chromium 模拟回归通过；真实 Cloudflare relay 和异网设备验收仍待完成。

## V0.7.2 — Cloudflare TURN Fallback

- 保留 V0.7.1 的 P2P 优先与联机玩法，新增 Cloudflare Realtime TURN 凭据 Worker 接入。
- 游戏仅请求 `https://api.feelpal.app/turn-credentials` 获取短期 ICE 配置；长期 TURN 凭据仅应保存在 Worker Secrets。
- 已加入 TURN URL 清理逻辑；强制 relay 自检只有实际收集到 `relay` candidate 才算通过。
- 此版本的实际 TURN relay 与异网真机验收仍待完成。

## V0.7.1 — PUBLIC NET / 公网联机稳定版

- 联机协议升级到 `crash-delivery-mp071-1`。
- 显式 STUN / TURN RTC 配置，支持自动、仅直连、强制 relay 三种策略。
- 新增网络设置页与 ICE candidate 自检。
- HUD 显示 P2P / TURN 及 RTT；暂停页显示链路状态。
- 客机断线自动重连最多 4 次，房主保留权威世界等待重新接入。
- 键盘、手机驾驶控件按下 / 松开立即发送输入，降低 50ms 周期同步带来的延迟。
- 房主处理离散动作后立即发送权威世界快照。
- 世界快照增加单调序号，客机丢弃陈旧快照。
- 默认不内置 TURN 私钥；用户可通过 UI 或 `window.CRASH_NETWORK_CONFIG` 提供自己的 TURN。
- 保留 V0.7 的三个城区、18 单、14 辆交通车、事故绕行、双人协作、昵称、车库与下车维修。

### 验证边界

- 本地双浏览器权威同步与 UI 回归通过。
- 执行环境原生 RTC 仍未产生 ICE candidate，不能据此宣称公网 NAT/TURN 真机通过。
- 使用真实 TURN 前应在「网络设置 → 测试 ICE / TURN」确认出现 `relay`。
